import styles from "../styles/HeaderPages.module.css";

function HeaderPages() {
  return (
    <header className={styles.HeaderPages}>
      {/* Header content will be handled by MenuTop component */}
    </header>
  )
}

export default HeaderPages;
